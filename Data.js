/**
 * Data.js — seed do controle anterior (projeto AG), NEUTRALIZADO.
 *
 * Esta planilha é uma cópia do controle que o Bruno mantinha antes. As 28
 * tarefas originais foram removidas junto com o restante dos dados legados
 * (Kanban → "Limpar dados antigos…", autorizado em 21/09/2026).
 *
 * A lista fica vazia de propósito: enquanto tivesse conteúdo, qualquer caminho
 * de instalação ou recarga poderia regravar na planilha exatamente o que
 * acabou de ser apagado. `seedTarefas` (Setup.js) verifica o tamanho desta
 * lista ANTES de limpar a base, então uma lista vazia não apaga nada.
 *
 * Para semear um projeto novo, use PlanoDocfinance.js / seedPlano, que insere
 * pelo domínio (com versão, auditoria e ID por projeto) em vez de escrever
 * direto nas células.
 */

var TASKS = [];
